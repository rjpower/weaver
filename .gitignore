target/
crates/loom/static/
static/dist/
*.sqlite
*.sqlite-shm
*.sqlite-wal
node_modules/
.playwright/
.playwright-mcp/
.claude/
.weaver/
.bot/
e2e/test-results/
*.pyc
python/weaver-loom/dist/
__pycache__/
python/weaver-loom/.venv/
python/weaver-loom/uv.lock

# Local-only container env: a halcyon-style deploy symlinks secrets/weaver.env
# to ./.env (holds GH_TOKEN etc.). Never commit it — it's host-specific secrets.
.env

# `loom setup`'s authored config — every credential in plaintext TOML. Never
# commit it; `loom.toml.example` (tracked) documents the shape.
loom.toml
